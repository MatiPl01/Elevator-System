export type ElevatorConfig = {
  minFloorNum: number,
  maxFloorNum: number,
  idleFloorNum: number,
  maxLoad: number,
  speed: number, // m/s
  stopDuration: number // seconds
  toggleDoorDuration: number, // seconda
}
